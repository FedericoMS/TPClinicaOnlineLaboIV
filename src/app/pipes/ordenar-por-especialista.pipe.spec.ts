import { OrdenarPorEspecialistaPipe } from './ordenar-por-especialista.pipe';

describe('OrdenarPorEspecialistaPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarPorEspecialistaPipe();
    expect(pipe).toBeTruthy();
  });
});
