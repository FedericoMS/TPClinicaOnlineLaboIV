import { OrdenarPorEspecialidadPipe } from './ordenar-por-especialidad.pipe';

describe('OrdenarPorEspecialidadPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarPorEspecialidadPipe();
    expect(pipe).toBeTruthy();
  });
});
