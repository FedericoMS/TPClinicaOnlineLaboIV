import { OrdenarPorPacientePipe } from './ordenar-por-paciente.pipe';

describe('OrdenarPorPacientePipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarPorPacientePipe();
    expect(pipe).toBeTruthy();
  });
});
